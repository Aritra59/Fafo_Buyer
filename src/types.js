/**
 * @typedef {{
 *   id: string,
 *   shopName?: string,
 *   name?: string,
 *   phone?: string,
 *   imageUrl?: string,
 *   isLive?: boolean,
 *   trialEnd?: unknown,
 *   isActive?: boolean,
 *   location?: { lat: number, lng: number }
 * }} Seller
 * @typedef {{
 *   id: string,
 *   sellerId: string,
 *   name?: string,
 *   price?: number,
 *   category?: string,
 *   imageUrl?: string,
 *   image?: string,
 *   prepTime?: number,
 *   prepTimeMinutes?: number,
 *   prepTimeMins?: number,
 *   preparationTime?: number
 * }} Product
 */

export {};
